services:
  wyandote-robotics-website:
    build: .
    container_name: team-website
    restart: unless-stopped

    ports:
      - "3000:3000"

    environment:
      NODE_ENV: production
      PORT: 3000

    env_file:
      - .env