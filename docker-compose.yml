services:
  website:
    build: .
    container_name: wyandotte-robotics-website
    restart: unless-stopped

    ports:
      - "3001:3000"

    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000

    env_file:
      - .env.local